# Normalize line endings: LF in the repo, native on checkout
* text=auto

# Always LF (executed inside Linux containers)
*.sh text eol=lf

# Binary — never touch
*.png binary
*.jpg binary
*.ico binary
*.woff2 binary
*.mp4 binary
